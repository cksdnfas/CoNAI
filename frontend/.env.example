# Optional: backend absolute base URL override (dev proxy is used by default)
# VITE_BACKEND_URL=http://localhost:1666

# Frontend dev server port is resolved from root .env FRONTEND_URL.
# Example root setting:
# FRONTEND_URL=http://localhost:1677
